version: '3'

services:
  db:
    image: postgres:15.3-alpine3.18
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devilofficial1337
      POSTGRES_DB: qtim_test
